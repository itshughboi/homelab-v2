> **DEPRECATED** — This playbook installs Docker + Portainer on a bare Ubuntu host.
> It was the original bootstrap approach before Athena was standardized as the management VM
> and all apps moved to Compose stacks managed through Semaphore.
>
> Kept for reference. Do not use for new deployments — provision Athena via Terraform
> instead and run the management stack Ansible playbook.
