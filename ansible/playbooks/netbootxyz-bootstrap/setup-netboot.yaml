---
- name: Configure Le Potato for netboot.xyz
  hosts: lepotato
  become: true
  # vars: # uncommented since I am pulling these from my repo instead of declaring them here
  #   docker_version: "20.10"  # optional, default latest works
  #   netbootxyz_image: "ghcr.io/netbootxyz/netboot.xyz:latest"
  #   netboot_container_name: "netbootxyz"
  #   netboot_ports:
  #     - "69:69/udp"
  #     - "80:80"
  #     - "4011:4011/udp"

  tasks:

    - name: Update apt cache
      ansible.builtin.apt:
        update_cache: true

    - name: Upgrade all packages
      ansible.builtin.apt:
        upgrade: dist
        autoremove: true

    - name: Install dependencies for Docker
      ansible.builtin.apt:
        name:
          - apt-transport-https
          - ca-certificates
          - curl
          - gnupg-agent
          - software-properties-common
        state: present

    - name: Add Docker GPG key
      ansible.builtin.shell: |
        curl -fsSL https://get.docker.com | sh
      args:
        creates: /usr/bin/docker

    - name: Ensure user is in docker group
      ansible.builtin.user:
        name: "{{ ansible_user }}"
        groups: docker
        append: true

    - name: Ensure Docker service is running
      ansible.builtin.systemd:
        name: docker
        state: started
        enabled: true

    - name: Install Docker Compose plugin
      ansible.builtin.apt:
        name: docker-compose-plugin
        state: present

    - name: Install git
      ansible.builtin.apt:
        name: git
        state: present

    - name: Clone homelab repository
      ansible.builtin.git:
        repo: "https://github.com/itshughboi/homelab-v2.git"
        dest: /opt/homelab
        version: main
        update: true

    - name: Run netbootxyz docker compose
      community.docker.docker_compose_v2:
        project_src: /opt/homelab/bootstrap/netbootxyz
        state: present