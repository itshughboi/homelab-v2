[all:vars]
ansible_user='hughboi'
ansible_become=yes
ansible_become_method=sudo
ansible_ssh_private_key_file=~/.ssh/id_rsa # change to corresponding key! Mac uses id_rsa. Ansible uses another key

[lepotato] 
10.150.1.13 # change IP here for its DHCP address.