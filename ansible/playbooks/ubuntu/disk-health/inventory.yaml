---
# Run against physical hosts only — VMs don't have direct SMART access.
# For Proxmox, SMART data is on the hypervisor node, not inside the VM.

all:
  vars:
    ansible_user: hughboi
    ansible_ssh_private_key_file: ~/.ssh/homelab-athena_id_ed25519

  hosts:
    # Proxmox hosts are root-only over SSH — no separate hughboi user exists there
    # (confirmed: `id hughboi` on pve-srv-1 returns "no such user"). Override per-host.
    pve-srv-1:
      ansible_host: 10.10.10.1
      ansible_user: root
      ansible_become: false
    pve-srv-2:
      ansible_host: 10.10.10.2
      ansible_user: root
      ansible_become: false
    pve-srv-3:
      ansible_host: 10.10.10.3
      ansible_user: root
      ansible_become: false
    pve-srv-4:
      ansible_host: 10.10.10.4
      ansible_user: root
      ansible_become: false
