---
# Replace with the IP of the new VM being bootstrapped.
# After bootstrap completes, move the host to your main inventory.

all:
  vars:
    ansible_user: hughboi
    ansible_ssh_private_key_file: ~/.ssh/homelab-athena_id_ed25519
    # On a brand-new VM you may need to use root and password auth for the first run:
    # ansible_user: root
    # ansible_password: "changeme"
    # ansible_ssh_common_args: '-o StrictHostKeyChecking=no'

  children:
    new_hosts:
      hosts:
        new-vm-01:
          ansible_host: 10.10.10.99    # replace with actual IP
