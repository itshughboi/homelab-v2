# This file should be encrypted with ansible-vault before committing.
#
# To encrypt: ansible-vault encrypt group_vars/vault.yml
# To edit:    ansible-vault edit group_vars/vault.yml
# To decrypt: ansible-vault decrypt group_vars/vault.yml  (careful — removes encryption)
#
# Generate a strong cluster secret:
#   openssl rand -hex 32
#
# This file is a TEMPLATE — fill in the value, then encrypt.

vault_k3s_cluster_secret: "REPLACE_WITH_OUTPUT_OF_openssl_rand_-hex_32"
