---
# Runs on one control-plane node — kubectl commands are cluster-wide.

all:
  vars:
    ansible_user: hughboi
    ansible_ssh_private_key_file: ~/.ssh/id_ed25519

  children:
    k3s_primary:
      hosts:
        k3s-master-1:
          ansible_host: 10.10.30.1
