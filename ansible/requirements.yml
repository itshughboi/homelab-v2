---
# Ansible collections required by the live playbooks (pinned).
# Install:  ansible-galaxy collection install -r ansible/requirements.yml
# Used by: Semaphore (Athena), laptop bootstrap runs, and CI (ansible-lint syntax-check).
collections:
  - name: community.docker
    version: ">=3.10.0,<5.0.0"
  - name: community.general
    version: ">=9.0.0,<11.0.0"
  - name: ansible.posix
    version: ">=1.5.0,<3.0.0"
