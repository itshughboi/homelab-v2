# Homelab TODO — 2026-06-11

Open items captured after the docs-bible solidification + GitOps/image-pinning work. The rebuild
docs now flag these honestly. Master rebuild runbook: [`docs/BUILD.md`](docs/BUILD.md).

---

## Actionable — do when ready

- [ ] **`terraform apply`** (`terraform/proxmox`) — applies the new **dock-prod VLAN-40 NIC**
      (`eth1` / `10.10.40.10`, MTU 9000). Then on dock-prod switch its fstab from `10.10.10.5` →
      `10.10.40.5` for jumbo NFS. *(Parent bridge `vmbr1` must be MTU 9000.)*

- [ ] **Fix the k8s NFS PV paths.** The PVs in
      `apps/kubernetes/k3s/apps/{jellyfin,immich,paperless-ngx,tube-archivist,file-browser}/storage.yaml`
      currently use `path: /mnt/truenas/<app>` — that's the dock-prod **client mountpoint**, not the
      server export. For k8s the `path:` must be the **server export path**:
      `"/mnt/The Archive/<Dataset>"` (quote the space) with `server: 10.10.40.5`. Map each to its real
      dataset (e.g. `Jellyfin`, immich = `Epoch-2/Pictures` + `Epoch-1/Legacy/Pictures`, paperless =
      `Epoch-1/Legacy/Documents/Financial`). See [`docs/4-storage/TrueNAS/README.md`](docs/4-storage/TrueNAS/README.md).

- [ ] **Finish image-tag pinning (B2 tail).** Last floating tags:
  - **hoarder → migrate to `ghcr.io/karakeep-app/karakeep`** (old `hoarder-app/hoarder` is abandoned;
    rename `HOARDER_*` → `KARAKEEP_*` env/secret).
  - **fasten-onprem** (`:main` only — pin a digest, or leave with a note).
  - Version-pin: paperless **tika**, **freshrss** (linuxserver `version-lsNNN`), **tube-archivist-es**,
    **unpoller**, **change-detection** (+ `browserless/chrome`). **unifi-controller** is test-only.

- [ ] **Sealed Secrets handoff.** Repo structure + a `kubeseal` runbook to run on Athena, so a rebuild
      needs no hand-typed app secrets (committed encrypted vs. today's imperative `kubectl create`).

- [ ] **Activate SOPS.** Run `./scripts/age-setup.sh` on Athena (fills the `AGE_PUBLIC_KEY_PLACEHOLDER`
      in `.sops.yaml`), then encrypt `secrets.sops.yaml` / `terraform.sops.tfvars`.

---

## Decisions to make (then update the docs)

- [ ] **dock-prod's future** — keep on Docker / migrate to k3s / unify. Drives the monitoring story
      (dock-prod ↔ k3s Loki, [`docs/7-k3s/Monitoring.md`](docs/7-k3s/Monitoring.md)) and the fate of
      **glances** + **promgraftail** (currently "status TBD" in [`docs/6-docker/index.md`](docs/6-docker/index.md)).

- [ ] **NTP** — implement on the UXG Max gateway. Runbook ready:
      [`docs/1-networking/Unifi/Networks/NTP.md`](docs/1-networking/Unifi/Networks/NTP.md). Small change,
      worth doing (Corosync / k3s / TLS are time-sensitive).

- [ ] **Bind9 secondary** — decide placement: k3s-replicated vs a dedicated VM on a different host.
      See "Target DNS Design" in [`docs/1-networking/Unifi/Networks/DNS.md`](docs/1-networking/Unifi/Networks/DNS.md).

- [ ] **Velero S3 target** — stand up MinIO on TrueNAS (bucket `homelab-velero`) so Velero DR is
      actually operational (currently labeled "not operational" in [`docs/7-k3s/index.md`](docs/7-k3s/index.md)).

---

## Nice-to-have / future

- [ ] **UPS + NUT** graceful shutdown — the one cheap hardware add worth reconsidering (guards against
      etcd/ZFS corruption on power loss).
- [ ] **TrueNAS pool expansion** — `The Archive` is ~95% full; planned 2nd SSD mirror to split personal
      (Epoch-1/2, Gaming, ios) from app data.
- [ ] **Run the DR drill** in [`docs/Backup-Recovery.md`](docs/Backup-Recovery.md) end-to-end at least once.
- [ ] **`terraform validate`** the `terraform/unifi` workspace once you have Terraform handy (it's not
      in CI; it's break-glass reference for the hand-configured UniFi).

---
*Generated 2026-06-11. Branch workflow note: pre-ArgoCD you can commit straight to `main`; once ArgoCD
is live, switch to branch → PR → merge ([`docs/8-gitops/Workflow.md`](docs/8-gitops/Workflow.md)).*
