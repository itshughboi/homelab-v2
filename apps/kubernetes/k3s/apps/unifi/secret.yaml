# Create imperatively — do NOT apply this file
# First create a read-only local user in the UniFi controller UI (not a Ubiquiti cloud account).
#
# kubectl create secret generic unifi-poller-env -n unifi \
#   --from-literal=UP_UNIFI_CONTROLLER_0_URL=https://unifi-controller:8443 \
#   --from-literal=UP_UNIFI_DEFAULT_USER=<readonly-username> \
#   --from-literal=UP_UNIFI_DEFAULT_PASS=<readonly-password> \
#   --from-literal=UP_UNIFI_CONTROLLER_0_PASS=<readonly-password> \
#   --from-literal=UP_UNIFI_CONTROLLER_0_INSECURE=true \
#   --from-literal=UP_POLLER_DEBUG=false
