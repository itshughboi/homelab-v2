# Create imperatively — do NOT apply this file
# kubectl create secret generic romm-env -n romm \
#   --from-literal=MYSQL_ROOT_PASSWORD=<password> \
#   --from-literal=DB_PASSWD=<password> \
#   --from-literal=ROMM_AUTH_SECRET_KEY=$(openssl rand -hex 32) \
#   --from-literal=IGDB_CLIENT_ID=<id-from-twitch-dev-console> \
#   --from-literal=IGDB_CLIENT_SECRET=<secret-from-twitch-dev-console> \
#   --from-literal=STEAMGRIDDB_API_KEY=<api-key-from-steamgriddb.com> \
#   --from-literal=RETROACHIEVEMENTS_API_KEY=<api-key-from-retroachievements.org>
#
# IGDB keys: https://api-docs.igdb.com/#account-creation (free Twitch dev account)
# SteamGridDB: https://www.steamgriddb.com/profile/preferences/api
# RetroAchievements: https://retroachievements.org/settings (Web API Key section)
