# Create imperatively — do NOT apply this file
# kubectl create secret generic hoarder-env -n hoarder \
#   --from-literal=NEXTAUTH_SECRET=$(openssl rand -hex 36) \
#   --from-literal=MEILI_MASTER_KEY=<password> \
#   --from-literal=OPENAI_API_KEY=""
#
# NEXTAUTH_SECRET must never change after first deploy — it invalidates all sessions.
