apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: hoarder
  namespace: hoarder
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`hoarder.hughboi.vip`)
      kind: Rule
      services:
        - name: hoarder
          port: 3000
  tls:
    secretName: hughboi-tls
