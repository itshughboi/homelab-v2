# Create imperatively — do NOT apply this file
# kubectl create secret generic renovate-env -n renovate \
#   --from-literal=RENOVATE_TOKEN=<gitea-personal-access-token> \
#   --from-literal=GITHUB_COM_TOKEN=<github-pat-for-changelog-lookups>
#
# RENOVATE_TOKEN: Gitea PAT with repo read+write (for creating PRs)
# GITHUB_COM_TOKEN: Optional GitHub PAT to avoid rate-limiting when Renovate
#   fetches changelogs from GitHub release pages (many Docker images link there)
