# Create imperatively — do NOT apply this file
# kubectl create secret generic cloudflare-token-secret -n cert-manager \
#   --from-literal=cloudflare-token=<cloudflare-api-token>
#
# Token requirements (Cloudflare dashboard → My Profile → API Tokens):
#   Zone: DNS:Edit
#   Zone: Zone:Read
#   Zone Resources: Include specific zone → hughboi.cc
