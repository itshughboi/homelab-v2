# DB password for the `grimmory` app user (grimmory + grimmory-db both read this)
DATABASE_PASSWORD=[CHANGEME]

# MariaDB root password (grimmory-db only)
MYSQL_ROOT_PASSWORD=[CHANGEME]

# Must match DATABASE_PASSWORD above — MariaDB provisions the app user with this on first boot
MYSQL_PASSWORD=[CHANGEME]
